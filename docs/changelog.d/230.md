- **Camera orientation.** Each craft can be mirrored and rotated; the choice sticks on this laptop and applies to the camera pane.

DECISION: Orientation lives in `localStorage` (`techtechflight:camera-orientation`), never on Telemetry — same class of school preference as the stream map. CSS `scaleX` + `rotate` is applied by the Integrator to the video element.
