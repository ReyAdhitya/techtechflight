- **Mission phase on the flight strip (`MissionPhaseBadge`).** Every phase reads in words with a distinct shape and semantic colour; an active exception adds its own words without changing the phase shape.
