- **Recall on the command row (`web/components/ControlScreen.tsx`).** Recall joins Land, Hover and Stop on every flight strip, enabled only while airborne. The receipt line reads sent, then waiting, then done from Telemetry alone — never from the button press.
