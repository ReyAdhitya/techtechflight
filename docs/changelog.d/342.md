- **Notes per pupil.** Free-text notes on each Student, saved when focus leaves the field — the same habit as craft notes.

DECISION: Pupil notes live in `techtechflight:pupil-notes`, not on `Logbook`, because this wave cannot extend the Logbook shape without colliding. Behaviour mirrors `writeNote`: trim, empty clears, `updatedAt` on save.
