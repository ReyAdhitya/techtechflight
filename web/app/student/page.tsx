'use client'

/**
 * The Student's Mission screen.
 *
 * A placeholder while the seam lands. The screen that draws arrives in the next commit;
 * the one that was reverted is not coming back.
 */
export default function StudentPage() {
  return (
    <main id="content" tabIndex={-1} className="p-8">
      <p className="m-0 text-body text-ink-muted">Waiting for the Teacher to open the classroom.</p>
    </main>
  )
}
