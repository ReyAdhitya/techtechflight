- **Safety brief.** Fixed classroom rules the Teacher can tick with the class; ticks reset when a new Lesson starts.

DECISION: Persist ticks in localStorage keyed to the running Lesson id (`techtechflight:safety-brief`), not on the Logbook — the brief is period chrome, not a Student record, and must not ride Telemetry or the optional Blob sync.
