- **Undo last assignment.** One step puts the previous Student ↔ Drone pairings back exactly.

DECISION: Undo depth is one snapshot of the whole live assignment map, captured before a mutation (`captureAssignmentUndoPoint` / `withAssignmentUndo`). The Integrator wraps assign paths; the button only restores. A second capture replaces the first — no multi-step stack.
