- **Mission clock.** Time remaining counts down from the Scenario limit in whole minutes; a Mission with no limit says so rather than showing zero, and one not yet started says "Not started". Crossing zero raises `mission-timeout` once via a rising-edge tracker, not every tick.
