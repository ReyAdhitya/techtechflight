- **Alert response options (`web/components/AlertResponseOptions.tsx`).** Each Alert lists its playbook responses as pressable choices in safety-priority order, with the recommended action named first so a Teacher never has to infer what an Alert implies.
