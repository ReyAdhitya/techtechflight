node_modules/
dist/
build/
.env
.env.local
*.log
.DS_Store
Thumbs.db
.vite/
coverage/
__pycache__/
.venv/
.scratch/
.next/
out/
*.tsbuildinfo
.vercel/
.claude/settings.local.json
# Screenshots from scripts/shot.mjs — evidence for one fix, stale by the next.
scripts/shots/

.vercel

# Personal coaching notes. Local only — never in a public history.
# Anchored to the root: `reference/` unanchored would also swallow a
# `web/reference/` someone adds later, which is not what this is for.
/NOTES.md
/MISSION.md
/RESOURCES.md
/lessons/
/learning-records/
/reference/

# Large ONNX weights — fetch via scripts/fetch-yolo-model.mjs
web/public/models/*.onnx

# Boss Classroom setup preference (Settings Sim vs Radio) — local laptop only
ground-station/classroom-source.json

