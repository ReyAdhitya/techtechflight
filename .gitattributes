# The feature dataset is tab-separated and parsed by scripts/. This repository is developed
# on Windows with core.autocrlf=true, which would hand those parsers CRLF rows; they cope,
# but a data file that means the same thing on both platforms is worth pinning outright.
*.tsv text eol=lf
