- **Roster CSV import.** Choose a class list file — a bad spreadsheet changes nothing and says why.

DECISION: Name-only CSV replaces the class via `saveRoll`. CSV with studentId upserts those rows and leaves other roster members alone. Validation runs to completion before any write.
